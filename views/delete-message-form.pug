extends layout

block content
 
    h1 #{title}
    p do you want to delete this message?
    p= message.title 
    p= message.text 

    form(method='POST',action='')
        div.form-group 
            input#messageid.form-control(type='hidden',name='messageid',required='true',value=message._id)
        button.btn.btn-primary(type='submit') Submit